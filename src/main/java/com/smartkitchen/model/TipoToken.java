package com.smartkitchen.model;

/** Para que sirve un {@link TokenCuenta}. */
public enum TipoToken {
    /** Confirmar la direccion de correo tras registrarse. */
    CONFIRMACION,
    /** Restablecer la contrasena ("He olvidado mi contrasena"). */
    RESET
}
