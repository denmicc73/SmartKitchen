# Los scripts de shell y los ficheros que consume Docker/Linux deben conservar
# finales de linea LF aunque se editen en Windows, o fallan en el VPS.
*.sh            text eol=lf
Dockerfile      text eol=lf
docker-compose*.yml text eol=lf
.env.example    text eol=lf
